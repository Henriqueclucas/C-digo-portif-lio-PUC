const circulo = require('.01a_definicaoCJS.js');
console.log(`A area do circulo de raio 4 é ${circulo.area(4)}`);

const {area} = require('01a_definicao.CJS');
console.log(`A area do circulo de raio 2 é ${area(2)}`);